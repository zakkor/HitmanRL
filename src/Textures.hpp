#ifndef TEXTURES_H
#define TEXTURES_H

#include <string>

namespace Textures
{
    enum ID : unsigned int
    {
        GRASS = 0,
        MUD,
        WALL,
        PLAYER,
        NPC,
        STONEFLOOR,
        EMPTY,
        DOOR,
        SHADOW,
        TERRAIN_ENUM_SIZE
    };
}

#endif

